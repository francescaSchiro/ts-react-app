import * as React from 'react';

import RedirectWrapper from './Wrapper';




const Redirect = () => (
    <RedirectWrapper>
      <div> Redirect </div>    
    </RedirectWrapper>
);

export default Redirect;