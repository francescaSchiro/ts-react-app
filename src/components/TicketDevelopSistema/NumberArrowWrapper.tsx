import styled from 'src/theme/default/index';

const NumberArrowWrapper = styled.div`

    display:flex;
    flex-direction: row;
    justify-content: flex-end;
    align-items: center;
`

export default NumberArrowWrapper;