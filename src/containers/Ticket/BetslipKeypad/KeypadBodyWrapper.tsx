import styled from 'src/theme/default/index';

const KeypadBodyWrapper = styled.div`
    width: 100%;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
`

export default KeypadBodyWrapper;